apply plugin: 'application'
mainClassName = 'com.twitter.zipkin.query.Main'

dependencies {
	compile zipkin('query-core', 'cassandra', 'redis', 'anormdb', 'mongodb')
}

sourceSets.test.resources {
	srcDir 'config'
}
