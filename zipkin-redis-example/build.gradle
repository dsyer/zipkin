apply plugin: 'application'
mainClassName = 'com.twitter.zipkin.example.Main'

dependencies {
	compile twitterServer,
			finagle('zipkin', 'stats'),
			zipkin('web', 'redis', 'query', 'receiver-scribe', 'zookeeper')
}
