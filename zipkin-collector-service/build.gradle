apply plugin: 'application'
mainClassName = 'com.twitter.zipkin.collector.Main'

dependencies {
	compile zipkin(
			'collector-core', 'collector-scribe', 'receiver-kafka',
			'cassandra', 'kafka', 'redis', 'anormdb', 'mongodb')
}

sourceSets.test.resources {
	srcDir 'config'
}
